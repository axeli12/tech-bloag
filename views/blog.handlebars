<div class="card text-center">
    <h2>{{blog_title}}</h2>
    <p>{{description}}</p>
    <p> Made By {{user.name}} on {{format_date date_created}}</p>
    <p>{{comment}}
      <button class="btn btn-primary">Click to add comment</button>
    </p>
</div>
